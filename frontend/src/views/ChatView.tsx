// Deprecated: replaced by components/ChatWidget.tsx (a floating panel over
// every tab instead of its own page) when the dashboard design was ported
// into this app. Safe to delete this file (and ChatView.css).
export {}
